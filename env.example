# 🔐 CONFIGURACIÓN DEL SERVIDOR FREEMIUM
# 
# Copia este archivo a .env y completa con tus claves reales

# Puerto del servidor
PORT=3000

# Clave secreta para JWT (generar una segura en producción)
JWT_SECRET=simulador_tactico_secret_change_in_production

# Claves de Stripe (obtener desde https://dashboard.stripe.com/apikeys)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Base de datos (opcional - para usar con MongoDB, PostgreSQL, etc.)
DATABASE_URL=mongodb://localhost:27017/simulador_tactico

# Entorno
NODE_ENV=development

# URL base del frontend (para CORS y redirects)
FRONTEND_URL=http://localhost:8080

# Configuración de email (opcional - para notificaciones)
EMAIL_SERVICE=gmail
EMAIL_USER=tu_email@gmail.com
EMAIL_PASS=tu_app_password

# Configuración de Redis (opcional - para cache y sessions)
REDIS_URL=redis://localhost:6379
